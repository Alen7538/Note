```c++
class Solution {
public:
    int hammingWeight(uint32_t n) {
        //return __builtin_popcount(n); 1、使用内置函数
        int i=32,count=0;
        while(i--){
            if(n&1==1) count++;
            n>>=1;
        }
        return count;
    }
};
```

